
* require を二度読み防止する
* stringをNULL終端ではなく、長さベースに
* 本物の多値
* syntax のシンボルを保護的に
* syntax を別オブジェクトに
* define-macro を scheme的に
* intを埋め込みに
* 継続をもうちょっと能率的に

- retainをCのポインタベースにする
- string stream
- 関数の引数チェックでエラーを出す
- string関係の関数
- syntaxのキーワード対応
- stringリテラルのエスケープ
- string表示のエスケープ
- デバッグ用の変数を指定する関数 (*runtime-option* "trace" 1) みたいな
- #!対応
- let と let*の区別
- define-syntax
- SRFI-1 list library
- 名前付きlet
- gcをまともに
- bundle を hashに
